var maxFreq = function(s, maxLetters, minSize, maxSize) {
  var tracker = {};
  //iterate string
  //  create blocks from minSize to maxSize
  //    if fits condition of maxLetters
  //      add to tracker

  for (var i = 0; i + minSize < s.length; i++) {
    for (var j = i; j + minSize < s.length; j++) {

    }
  }
};